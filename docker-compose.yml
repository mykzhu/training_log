services:
  training-log:
    build: .
    container_name: training-log
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
    environment:
      DB_PATH: /data/training.db
    restart: unless-stopped
